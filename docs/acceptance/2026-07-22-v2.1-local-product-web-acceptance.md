# v2.1 Local Product Web 验收

## 结论

本地 Product Web 候选版本通过功能、真实数据、响应式布局和常驻运行验收，可进入 PR/CI 门禁。该结论不包含 ChatGPT Sites、公网部署或移动网络访问。

## 验收基线

- 代码分支：`codex/v2-web-console-next`
- 核心 UI commit：`4113e67`
- 静态打包 commit：`61886eb`
- 本地服务 commit：`adc9ad8`
- 导航一致性 commit：`df927fc`
- 应用版本：`2.1.0rc1`
- URL：`http://127.0.0.1:8768`
- 监听边界：`127.0.0.1`，未绑定公网接口

## 真实数据验收

数据读取自主工作区最新 `outputs/market/market_intelligence_report.json`：

- provider：`akshare`
- as_of：`2026-07-21`
- 全市场记录：21,570
- ETF：1,552
- 代码搜索 `021511`：返回“宏利半导体产业混合发起C”
- 名称搜索“人工智能”：返回 120 条
- 主题筛选“半导体”：返回 90 条
- 详情接口保留 source、as_of、quality 和 missing fields，不把缺失字段转为正向信号

## 功能验收

- “全市场 / 我的自选”是两个明确范围；自选池显示 3 只配置基金。
- 支持代码、名称、类型、主题、ETF、数据质量、收益排序和服务端分页。
- 详情抽屉显示基金结构化证据、数据来源、日期、质量与缺失字段。
- 研究总览、市场情报、基金探索、组合分析、新闻证据、研究助手、人工审核、报告中心 8 个入口均可访问。
- 页面不修改主评分或主风险，不输出买卖建议，不触发交易。

## 浏览器验收

| 视口 | 页面级横向溢出 | Console error/warning | 证据 |
| --- | --- | --- | --- |
| 1440x1000 | 无 | 0 / 0 | `outputs/acceptance/v2.1/fund-explorer-desktop.png` |
| 768x1024 | 无 | 0 / 0 | `outputs/acceptance/v2.1/fund-explorer-tablet.png` |
| 375x812 | 无 | 0 / 0 | `outputs/acceptance/v2.1/fund-explorer-mobile.png` |

桌面表格的横向滚动被约束在数据表容器内，不扩大页面宽度；手机端导航按钮有可访问名称，侧栏关闭时使用 `inert` 和 `aria-hidden`。

## 工程验证

- Python：458 passed，1 skipped。
- Python compileall：通过。
- React：12 个测试文件、25 个测试通过。
- TypeScript typecheck：通过。
- Vite production build：通过。
- wheel 构建：通过，包含 `fund_agent/web_static/index.html` 和所有 hashed assets。
- contract validation：7 个当前 artifact 通过。

## 常驻运行验收

- LaunchAgent：`com.ya-fundmind.web`
- installed：true
- loaded：true
- health reachable：true
- home reachable：true
- `RunAtLoad=true`、`KeepAlive=true`
- daily scheduler：installed / loaded
- weekly scheduler：installed / loaded
- Web 服务独立安装，不修改 daily/weekly 时间、provider、watchlist 或 portfolio。

## 延期范围

ChatGPT Sites、公网部署、账号系统、云端同步和外网移动访问延后评估。当前 outputs 保留在本机，不上传到第三方站点。
