# v2.2 Fund Data Terminal M2 验收记录

## 结论

`pass`。市场页已具备上证指数、沪深 300、创业板指的按需历史日线浏览能力，支持 `1m / 3m / 6m / 1y / all`、新鲜缓存、live 获取、stale fallback、数据来源和质量状态。

本记录只代表 `v2.2 M2` 通过，不代表 `v2.2.0` 已发布。行业板块走势、整体数据终端重组和最终发布仍按 M3-M5 继续执行。

## 实现范围

- `MarketSeriesPoint`
- SQLite `market_series`
- `AkshareProvider.fetch_index_history`
- `MarketHistoryService`
- `GET /api/market/indices`
- `GET /api/market/indices/{symbol}/history`
- React 主要指数切换、历史窗口、折线图和可折叠 OHLC 数据表

## 真实 AKShare Smoke

日期：2026-07-23

| symbol | 名称 | 有效点 | 最新日期 | 最新收盘 |
| --- | --- | ---: | --- | ---: |
| `000001` | 上证指数 | 132 | 2026-07-22 | 3867.034 |
| `000300` | 沪深300 | 132 | 2026-07-22 | 4717.244 |
| `399006` | 创业板指 | 132 | 2026-07-22 | 3566.732 |

本次指定 2026-01-01 至 2026-07-23。当前网络下 `stock_zh_index_daily_em` 均真实失败，`stock_zh_index_daily` 均真实成功。provider health 保留了两个 endpoint trace 和 `endpoint_fallback` warning，没有伪造主端点成功。

API 验证：

- 沪深 300 `6m`：120 点，首次 `source=akshare`。
- 最新点：`2026-07-22 / 4717.244`。
- `stale=false`、`fallback_used=false`、`data_quality_grade=normal`。
- 第二次 `1m`：20 点，`source=cache:akshare`。

这里的 endpoint fallback 只是在 AKShare provider 内切换数据端点；`fallback_used` 专指 live provider 到 SQLite 缓存的降级，因此保持 `false`。

## Web 验收

- 1440x1000：指数、窗口、摘要、曲线和下方主题表层级正常。
- 768x1024：指标区为 2 列，曲线完整，无横向溢出。
- 375x812：指数和窗口控件各自成行，摘要为 2x2，曲线宽度 313px，无横向溢出。
- 手机端 `cache:akshare · 2026-07-22` 完整换行显示，不隐藏来源日期。
- 上证指数切换与 `1y` 窗口切换成功，显示 240 个交易点。
- 独立审查发现并修复 Market Intelligence 产物缺失时误隐藏指数面板的问题；主题区域降级为空状态时，指数 API 和曲线仍可使用。
- 浏览器 console：0 error，0 warning。

## 自动验证

- Python pytest：`489 passed, 1 skipped`
- Python compileall：通过
- React Vitest：`29 passed`
- TypeScript typecheck：通过
- Vite production build：通过
- output contract validation：通过

## 边界确认

- 修改主评分：没有
- 修改主风险：没有
- 修改 daily 默认 provider：没有
- 修改 watchlist / portfolio：没有
- 修改 scheduler：没有
- 多源自动核验：没有
- 输出板块推荐或买卖建议：没有
- 自动交易或券商接入：没有

## 下一步

1. M3：行业板块时间序列、板块历史 API 和板块详情曲线。
2. M4：把页面重组为行情总览、基金终端、组合和研究证据四个主工作区。
3. M5：完整真实 smoke、三视口验收、CI、版本、回滚和 `v2.2.0` 发布。
