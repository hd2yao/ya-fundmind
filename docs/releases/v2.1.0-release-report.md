# v2.1.0 Release Report

## 结论

YA FundMind OS `v2.1.0` 完成本地 Product Web 与全市场 Fund Explorer 交付门禁。正式 tag 在本报告合入 clean main 后创建。

## 用户可见能力

- 本地地址：`http://127.0.0.1:8768`
- 八个工作区：研究总览、市场情报、基金探索、组合分析、新闻证据、研究助手、人工审核、报告中心。
- 全市场基金与 ETF 支持代码、名称、类型、主题、ETF、质量、收益排序和服务端分页。
- “全市场”和“我的自选”明确分区，搜索结果不表示推荐。
- 基金详情展示结构化证据、source、as_of、质量和缺失字段。
- daily/weekly 更新 JSON 后页面读取最新数据，不要求重新构建前端。

## 发布证据

- PR #32：合并。
- RC tag：`v2.1.0rc1`。
- Python：459 passed，1 skipped；compileall 通过。
- CI：Python 3.10、Python 3.12、Product Web 通过。
- contracts：当前 7 类 artifact 通过。
- 真实数据：AKShare 21,570 条，as_of `2026-07-21`。
- 浏览器：375x812、768x1024、940x900、1440x1000 无页面级横向溢出；console 0 error / 0 warning。
- 本地服务：LaunchAgent installed/loaded，health 与首页可访问，工作目录指向 main。
- daily/weekly：保持 installed/loaded，调度配置未改。

## 工程交付

- FastAPI 只读 API 与 React 19 前端。
- `fund_agent/web_static` 随 Python wheel 分发。
- `com.ya-fundmind.web` 仅监听 `127.0.0.1:8768`，`RunAtLoad` 与 `KeepAlive` 生效。
- 原 Streamlit `web-console` 保持兼容回退。

## 边界

- 没有修改主评分或主风险。
- 没有修改 provider default、watchlist、portfolio 或 daily/weekly schedule。
- 不自动交易，不接券商，不输出买卖建议或收益承诺。
- ChatGPT Sites、公网部署、账号系统和云端同步延期，不上传本地 outputs。

## 回滚

Web 服务可用 `bash scripts/uninstall_local_product_web.sh` 独立卸载，不删除 outputs，也不影响 daily/weekly。代码回滚使用上一稳定 tag `v2.0.0`；数据产物不需要迁移。
