# YA FundMind OS v2.0.0 Release Report

## 发布结论

YA FundMind OS V2 已完成 M1-M6 与 Final 发布门。V2 把 V1 的自动研究产物收敛为本地、证据驱动、只读 Research Copilot，并保持 daily/weekly、主评分、主风险和研究边界不变。

本报告随 Final 分支提交。Final PR、CI、merge commit 和 tag 由本文件所在提交的远端发布门补齐；tag 只能在 clean `main` 完成 post-merge smoke 后创建。

## Post-RC 真实运行门

权威文件：`outputs/release/v2_release_readiness.json`。

| 日期 | App | Commit | Trigger | AKShare live rows | 数据质量 | Fallback | Strict contracts |
| --- | --- | --- | --- | ---: | --- | --- | --- |
| 2026-07-15 | `2.0.0rc1` | `aaf526f` | `daily_ops` | 19,987 | warning | false | pass |
| 2026-07-16 | `2.0.0rc1` | `aaf526f` | `daily_ops` | 21,546 | normal | false | pass |
| 2026-07-17 | `2.0.0rc1` | `aaf526f` | `daily_ops` | 21,536 | normal | false | pass |

三个 run 均满足：

- 精确 commit `aaf526fa6d67b6933a67b908021df9419a83c786`。
- `git_dirty=false`，没有用未提交代码生成发布证据。
- provider 为 AKShare live，mapped rows 等于 live rows。
- 无 fallback、critical warning 或 degraded quality。
- report、snapshot、trace strict contracts 通过。
- `not_production_model=true`、`main_score_changed=false`、`main_risk_changed=false` 边界由 run contract/readiness 校验。

最终 readiness 为 `status=pass`、`valid_run_count=3/3`、`blockers=[]`。权威 readiness 于 `2026-07-17T14:37:39.330682+00:00` 生成；Catalog、Market Query、Market Answer 均在 750/1200/2000 ms 预算内。精确耗时以会被每次复核更新的权威 JSON 为准。

## Final 交付范围

- Artifact Catalog 与兼容旧 schema 的结构化读取。
- Evidence/Citation、数据质量门和跨来源冲突标记。
- market、fund、portfolio、news、history、quality 六类 Research Query/Copilot。
- deterministic no-LLM 回答、引用、data gaps、warning 和拒答边界。
- 只读 Skill 与 optional MCP 五工具接口。
- 本地 Streamlit Copilot Console 与人工审核入口。
- release-readiness、性能预算、安全/隐私矩阵和 Python 3.10/3.12 CI。
- V1 daily/weekly、dashboard、report、snapshot、trace、market、fund detail、portfolio 和 news evidence 继续兼容。

产品化 React/FastAPI Web Console 保持在独立 Draft PR #32，不进入 `v2.0.0`，避免改变 RC provenance 和 Final 范围。

## Final 验证

### Python、契约和安装包

- 全量 pytest：`415 passed, 1 skipped`；skip 为默认环境未安装 optional MCP integration。耗时会随复核环境变化，不作为 release gate。
- release/contract/security/compatibility/performance/E2E 聚焦矩阵：`69 passed`。
- `python -m compileall -q fund_agent`：通过。
- 主工作区 `validate-contract --output-dir outputs`：report、snapshot、trace、research context、evidence、answer、release readiness 全部通过。
- 从 release branch 构建 `ya_fundmind-2.0.0-py3-none-any.whl`，METADATA 与 `fund_agent.__version__` 均为 `2.0.0`；全新虚拟环境安装和 `fund-agent --help` 通过。

### Daily、Weekly 与六类 CLI

在全新 `/tmp` 输出目录运行 fixture daily/weekly：两者 exit 0，ops status 为 `ok`，dashboard 与 contracts 可读。单日 fixture 历史不足按设计保留 `insufficient_history`，不影响研究和展示链路。

| Topic | Answer status | Findings | Evidence | 说明 |
| --- | --- | ---: | ---: | --- |
| market | answered | 9 | 9 | 结构化市场证据可用 |
| fund | partial | 14 | 14 | fixture 字段不足被如实降级 |
| portfolio | answered | 6 | 6 | 组合观察证据可用 |
| news | answered | 6 | 6 | 新闻/公告 fixture 证据可用 |
| history | partial | 1 | 1 | 单日历史不足被如实降级 |
| quality | answered | 8 | 8 | 数据质量证据可用 |

“请替我买入 510300 并保证收益”返回 `blocked_transaction/refused`，findings/evidence 均为 0，并保留 `read_only_boundary_enforced`。

### Optional MCP

- 全新 Python 3.12 虚拟环境安装官方 `mcp 1.28.1` 与本 wheel source。
- MCP adapter/contract/gateway/integration/optional dependency/server CLI/safe-I/O：`47 passed`。
- `mcp-server --dry-run`：仅暴露 `status, catalog, query, ask, evidence`，`read_only=true`。

### Web 与本地运维

- Streamlit `1.59.2` 启动成功并读取真实主工作区 outputs。
- Home、Copilot、Market、Funds、Portfolio、News、Review、Reports 在 375x812、768x1024、1440x1000 共 24 组均有非空内容。
- 24/24 页面级横向 overflow 为 0；浏览器 console errors/warnings 为 0；三张 full-page 截图已人工检查，验收文件保留在 `/tmp`，不提交仓库。
- Streamlit 服务端出现 `use_container_width` 未来弃用提示，属于 P2，未影响本次 UI 或运行结果。
- daily/weekly launchd 均 installed/loaded；最新 run 为 2026-07-17，ops status `ok`，dashboard 可用。
- 在 clean RC `main` 上再次执行 readiness：`status=pass`、3/3、contracts/performance 均通过。
- Final PR [#33](https://github.com/hd2yao/ya-fundmind/pull/33) 首轮 CI 已通过 Python 3.10/3.12；默认 CI 按设计跳过依赖真实网络的 AKShare/Tiantian smoke。

Final merge commit、tag 和 post-release ops check 在远端发布门完成后补入本报告；这些步骤完成前不得创建 tag。

## 回滚

V1 artifact 和 CLI 保持兼容，不需要数据迁移。若 Final post-release check 发现 P0/P1：

1. 保留 outputs、runs、cache、scheduler 和 review state。
2. 从 tag 创建非破坏性回滚分支：`git switch -c rollback/v2.0.0-rc.1 v2.0.0-rc.1`；不在 `main` 上 reset，也不改写历史 run metadata。
3. 根因修复走独立 patch PR、CI 和新 tag。
4. 不通过放宽 contract、伪造日期或启用 fallback 绕过发布门。

## 边界确认

- 没有修改主评分。
- 没有修改主风险。
- 没有改变 provider default、watchlist、portfolio 或 scheduler schedule。
- 没有自动交易或券商接入。
- 没有买卖建议或收益承诺。
- LLM 不是 V2 运行依赖。
