# v2.2 Fund Data Terminal M1 验收记录

## 结论

`pass`。单基金历史净值的按需数据闭环已完成，可以从全市场搜索结果打开基金详情并查看 `1m / 3m / 6m / 1y / all` 历史曲线。

本记录只代表 `v2.2 M1` 通过，不代表 `v2.2.0` 已发布。指数走势、板块走势、整体数据终端改版和最终发布仍按设计文档继续执行。

## 实现范围

- `AkshareProvider.fetch_nav_history`
- `FundHistoryService`
- SQLite `fund_navs` 来源隔离读取
- `GET /api/funds/{code}/history`
- React 基金详情历史净值曲线
- 新鲜缓存、live fetch、stale fallback 和可解释 503

## 真实 AKShare Smoke

日期：2026-07-23

基金：

- code: `021511`
- name: `宏利半导体产业混合发起C`
- endpoint: `fund_open_fund_info_em`
- window: `6m`
- point_count: `120`
- first_point: `2026-01-21 / 2.2078`
- latest_point: `2026-07-22 / 2.9481`
- source on first request: `akshare`
- source on second request: `cache:akshare`
- stale: `false`
- fallback_used: `false`
- data_quality_grade: `normal`

首次 live 请求约 `0.95s`；第二次新鲜缓存请求约 `0.01s`。没有伪造真实 smoke 成功。

验收中发现 daily 基础净值写入的单点记录会被误判为完整历史。修复后，只有 `metadata.series_kind=fund_nav_history` 的记录才会命中历史缓存。

## Web 验收

- 1440x1000：详情抽屉、120 点曲线、状态摘要正常。
- 768x1024：无横向溢出。
- 375x812：详情全屏，窗口控件、曲线和 2x2 摘要正常。
- 1 月窗口切换后显示 20 个净值点。
- 浏览器 console：0 error，0 warning。
- 页面不覆盖主评分或主风险，不显示买卖建议。

## 自动验证

- Python pytest：`472 passed, 1 skipped`（最终验证以分支最新 CI 输出为准）
- Python compileall：通过
- React Vitest：`26 passed`
- TypeScript typecheck：通过
- Vite production build：通过

## 边界确认

- 修改主评分：没有
- 修改主风险：没有
- 修改 daily 默认 provider：没有
- 修改 watchlist / portfolio：没有
- 修改 scheduler：没有
- 多源自动核验：没有
- 输出买卖建议：没有
- 自动交易或券商接入：没有

## 下一步

1. M2：主要指数日线、独立市场序列缓存/API 和市场页指数图表。
2. M3：行业板块日线和板块详情。
3. M4：将整体前端重组为行情总览、基金终端、组合和研究证据四个主工作区。
4. M5：完整真实 smoke、三视口验收、CI、版本和回滚。
