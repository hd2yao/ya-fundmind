# v2.1.0rc1 Release Report

## 状态

候选实现和本地验收已完成。PR #32 已合并，clean main tag `v2.1.0rc1` 已推送。

## 交付范围

- React 19 + FastAPI Product Web 正式进入 v2.1 版本轨道。
- 全市场 Fund Explorer 支持代码、名称、类型、主题、ETF、质量、排序和服务端分页。
- 单只基金详情合并市场记录和已有 enrichment，过滤本地路径和非 allowlist metadata。
- “全市场 / 我的自选”双视图明确区分搜索范围。
- 静态构建进入 `fund_agent/web_static` 并随 wheel 分发。
- 独立 launchd 服务默认监听 `127.0.0.1:8768`。

## 数据与安全边界

- 浏览器不加载全部市场记录，每次最多返回 100 条。
- 搜索 API 不接受任意 path、URL、provider 或 SQL。
- TrustedHost、loopback Origin、report allowlist 和 review canonical id 规则保留。
- 主评分、主风险、provider default、watchlist、portfolio 和 daily/weekly schedule 不变。
- 不自动交易、不接券商、不输出买卖建议或收益承诺。

## 已完成验证

- Fund Explorer index/API focused tests。
- Product Web CLI、V2 security 和 E2E focused tests。
- React typecheck、25 个 Vitest 测试和 production build。
- `pip wheel --no-deps` 构建成功，wheel 包含 `web_static/index.html` 与全部 assets。
- launchd template、dry-run、scheduler isolation 和 shell syntax 检查。
- 全量 pytest：459 passed，1 skipped；`compileall` 通过。
- 当前 outputs 的 7 类 contract artifact 全部通过校验。
- 真实 AKShare Market Intelligence 索引包含 21,570 条记录，代码、名称和主题搜索通过。
- 375x812、768x1024、1440x1000 三视口无页面级横向溢出，浏览器 console 0 error / 0 warning。
- 940px 中间断点导航回归验收通过，CSS/JavaScript 均使用 960px 响应式边界。
- 本地 launchd 已安装并加载，health 与首页可访问；daily/weekly scheduler 均保持 loaded。

## 发布结果

- 对抗式评审无未解决 P0/P1。
- Python 3.10、Python 3.12 和 Product Web CI 通过。
- merge commit：`d307072debba98799928cfab59cddee6ac4a7e75`。
- RC tag：`v2.1.0rc1`。
